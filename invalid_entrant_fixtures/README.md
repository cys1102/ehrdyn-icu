# Invalid entrant fixtures

`invalid.py` is parameterized by `INVALID_MODE` for the focused validation
tests: `malformed`, `nonfinite`, `unsupported`, `negative`, `nonnormalized`,
`wrong_dimension`, `slow`, `crash`, and `nondeterministic`. These fixtures are
test-only and are never scored as entrants.
