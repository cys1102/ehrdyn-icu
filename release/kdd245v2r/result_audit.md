# KDD245V2R result audit

Decision:
`complete_public_canonical_v2_recursive_ehr_scorer_release`.

The candidate binds the exact public constructor source to the separately
authorized KDD220M2R2A scientific surface. The original KDD220M2R2 stop remains
unchanged. No MIMIC data or MIMIC-derived scientific result bundle is included.
The author-side scientific authority is the immutable R2A-adjudicated surface;
KDD245V2R performed no clinical-data access and no scientific recomputation.

The implemented scorer covers five-task one-step and recursive prediction,
point/Gaussian/ensemble uncertainty where defined, termination, and
retrospective support/evaluability diagnostics. Planning, direct return, known
policy value, treatment benefit, causal effects, clinical utility, and
independent validation remain unsupported.

Verification passed 14/14 focused scorer tests and 147/147 complete public
tests under Python 3.11. The focused suite, all 13 Draft 2020-12 schemas, and
the synthetic Gaussian quickstart passed clean installs under Python 3.11,
3.12, and 3.13. The canonical Gaussian output hash was identical across all
three interpreters. Independently calculated point, Gaussian, and ensemble
fixture metrics matched within the frozen tolerance.

The complete checksum manifest, documentation links, citation, redistribution
inventory, privacy scan, prohibited-field scan, and diff checks passed. The
annotated tag and public Release use the immutable `v2.0.0` identity. Exact
commit, asset, uploaded/downloaded checksum, and replay evidence are reported
by the public Release receipt and its aggregate verification asset.

Author-side execution is reproducibility evidence only. Clean-clone and
downloaded-archive replay establish software portability, not a genuine
non-author reconstruction. KDD220C10R is authorized as a separate future
credentialed execution; it is not simulated here.
