# KDD220C10R handoff

KDD220C10R is authorized only after KDD245V2R has the terminal decision
`complete_public_canonical_v2_recursive_ehr_scorer_release`, an immutable
annotated `v2.0.0` tag, a public GitHub Release, verified assets, and a passing
downloaded-archive replay.

A qualifying executor must be a genuine non-author using their own authorized
MIMIC-IV v3.1 access and public materials only. Author-side execution,
clean-clone portability testing, and this handoff are not independent
reconstruction.

The executor must reconstruct once, freeze aggregate outputs before opening
the public expected manifest, run every capability marked Yes, retain
Structural N/A outputs, and return only privacy-reviewed aggregate receipts.
