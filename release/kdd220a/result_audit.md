# KDD220A result audit

KDD220A preserved both stopped KDD217 evidence trees and source-closed the
previously missing 96-hour raw extraction parameter from the explicitly
authorized aggregate configuration receipt. The value is now frozen in public
source and validated as a raw buffer distinct from the 72-hour/18-bin episode.

The source-hash-gated synthetic differential passed for sepsis, respiratory
support, shock, AKI, and heart failure. It covered final anchors, role logic,
KDD201 temporal retention, ordered transitions, SAFE interface indexing,
K25/K4/K2 action classes, dense and terminal reward masks/timing, and exactly
one terminal step. Maximum observed floating difference was zero under the
frozen `rtol=1e-12`, `atol=1e-12` gate.

No MIMIC data, credential, patient row, restricted array, split manifest,
checkpoint, model output, or historical cohort count was accessed. The only
historical result read was the allowlisted aggregate backend configuration
receipt. KDD220A therefore establishes a source-closed synthetic candidate,
not credentialed real-EHR reconstruction or independent reproduction.

The current public runtime has zero result/artifact/private-path dependencies.
The historical seven-task SQL workflow remains preserved but is explicitly
superseded for the current five-task candidate.
