# KDD220AR2 result audit

The corrected dual-identity gate passed for the preserved 16-file KDD220B
receipt. KDD220A and KDD220B artifacts were not modified. The earlier KDD220AR
nonexecution remains a prompt-identity-specification failure and is not
reclassified as a completed experiment.

Authoritative source inspection showed that RV01R removes missing, equal, and
reversed ICU time windows before downstream construction. KDD121 separately
retains its existing 24-hour task-specific eligibility. KDD220AR2 changes only
the public global-abort behavior: invalid time-order rows are now excluded with
mutually exclusive aggregate reasons before anchors and downstream interfaces.

The official-column-shape fixture retained the valid one-hour stay unchanged,
excluded one row in each invalid category, and preserved hard failures for a
nonempty malformed timestamp and duplicate stay identifier. The five-task
synthetic aggregate receipt remained byte-identical to KDD220A, and all prior
anchor, transition, SAFE-feature, action, reward, and termination gates passed.

No MIMIC data, credential, patient row, restricted array, historical cohort
count, checkpoint, model output, or row-aligned reference was accessed. This is
a public runtime repair ready for KDD220BR credentialed parity, not evidence of
credentialed reconstruction.
