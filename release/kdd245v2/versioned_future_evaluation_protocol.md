# Versioned future evaluation protocol

No v2 evaluator version is released by this stopped stage. A future rerun must
bind its evaluator, typed schemas, canonical serialization, horizons, metric
definitions, suppression rules, task-contract ID, constructor commit, and
complete KDD220M2 evidence identity before any entrant result is inspected.
Mixed v1/v2 lineage artifacts must fail closed.
