# KDD220M2 and KDD245V2 authorization

KDD220M2 is authorized from the exact committed KDD220M1 canonical manifest.
It must recompute every affected real-EHR consumer in
`affected_task_and_artifact_refresh_manifest.csv` from the frozen candidate-v2
inputs. It must also emit disclosure-safe per-feature observation rates, mask and
recency summaries, target availability, train-only normalization summaries, and
selected prespecified quantiles that were not emitted by the reused BR8R2 receipt.
No retained historical number may be used as a tuning target.

KDD245V2 software implementation is authorized to bind its scorer schemas and
synthetic tests to the frozen v2 task contract. Accepted real-EHR parity claims and
publication remain blocked until the required v2 aggregate refresh is complete.

This authorization does not cover constructed-environment reruns, manuscript edits,
tagging, publishing v2.0.0, or independent reconstruction claims.
