stop_source_test_constructor_identity_or_privacy_failure

The null-safe source repair and all software-only verification gates passed, but
the single authorized constructor invocation stopped before reading clinical
rows because the supplied input layout exposed more than one encoding for a
required table. No seven-file candidate was produced, so canonical-v2
scientific equivalence was not evaluated and KDD220M2R is not authorized.
